The following folders will be created if not present. If you
already have these kinds of folders, use symlink (or "Alias")
to link to them here, but make sure to use these exact names
which ARE case-sensitive.

These folders are in the .gitignore, so they'll never
show up as modified in git-status.

Movies
TV
Music
Complete
Incomplete
NZB
